<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>KHost.Spike.SyncHarness</RootNamespace>
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <!-- Real IPC and real domain services: the point is to exercise the production sync path with
       two real Screen2 processes, without needing a seeded media library. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\src\KHost.IPC.SignalR\KHost.IPC.SignalR.csproj" />
    <ProjectReference Include="..\..\..\src\KHost.Domain\KHost.Domain.csproj" />
  </ItemGroup>

</Project>
