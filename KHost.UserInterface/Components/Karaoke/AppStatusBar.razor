<span class="kh-app__toast-message">@_toastMessage</span>
<span class="kh-app__version">Version: @_version</span>

@code {
    private string _version = "_ERROR_";
    private string _toastMessage = "Sing that Media Comrade!";

    protected override void OnInitialized()
    {
        var assembly = System.Reflection.Assembly.GetExecutingAssembly();
        var version = assembly.GetName().Version;
        _version = version?.ToString() ?? "_ERROR_";

    }

    public void SetToastMessage(string message)
    {
        _toastMessage = message;
        StateHasChanged();
    }
}
