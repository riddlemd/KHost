@if (IsOpen)
{
    <div class="kh-scrim" @onclick="OnClick">
        @ChildContent
    </div>
}

@code {
    [Parameter] public bool IsOpen { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }
}
