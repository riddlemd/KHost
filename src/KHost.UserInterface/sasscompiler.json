{
  "$comment": "Directory mode compiles app.scss and each themes/*.scss; everything else is a _partial and is skipped. Scoped css must be generated here rather than from an MSBuild target, because Blazor discovers it from evaluation-time items.",
  "Source": "wwwroot/scss",
  "Target": "wwwroot/css",
  "GenerateScopedCss": true,
  "ScopedCssFolders": [ "Components" ],
  "Arguments": "--style=compressed --no-source-map"
}
