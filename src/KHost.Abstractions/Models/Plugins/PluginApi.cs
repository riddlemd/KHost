namespace KHost.Abstractions.Models.Plugins;

public static class PluginApi
{
    /// <summary>Bumped only on breaking changes to the plugin-facing contracts.</summary>
    public const int CurrentVersion = 1;
}
